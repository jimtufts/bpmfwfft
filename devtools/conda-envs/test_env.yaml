name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - setuptools

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # BPMFwFFT depends
  - numpy
  - netcdf4
  - cython
  - mdtraj
  - pyfftw
  - openmm
  - eigen
  - versioneer

    # Pip-only installs
  #- pip:
  #  - codecov

