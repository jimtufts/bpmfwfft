name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - setuptools

    # Build tools
  - c-compiler
  - cxx-compiler
  - make

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # BPMFwFFT depends
  - numpy
  - netcdf4
  - cython
  - mdtraj
  - pyfftw
  - openmm
  - eigen
  - versioneer

    # Pip-only installs
  #- pip:
  #  - codecov

