name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # BPMFwFFT depends
  - numpy
  - netcdf4
  - cython
  - mdtraj
  - pyfftw
  - openmm

    # Pip-only installs
  #- pip:
  #  - codecov

